@using Sandbox.UI;

@attribute [StyleSheet]
@namespace Bydrive
@inherits PanelComponent

<root class="@UI.ActiveIf(!string.IsNullOrEmpty(Text))">
	@if(!string.IsNullOrEmpty(Name))
	{
		<label class="name">@Name</label>
	}
	<label class="message">@GetCurrentText()</label>
</root>